import React from 'react';

const SectionTitle = ({title}) => {
  return (
    <div className="section_service_2">
      <h1 className="service_text">{ title }</h1>
    </div>
  );
};

export default SectionTitle;